def val_phone(m):
    """Phone number validation function."""
    m = re.sub(r"\D", "", m)
    valid = True
    # this tests to match the starting numbers with the rest of the number
    if re.match(r"^(64|04|02)", m):
        # if the number starts with 64 the total amount of letter have to be 10-13
        if re.match(r"^64", m) and (len(m) < 10 or len(m) > 13):
            valid = False
        # if the number starts with 04 the total amount of letter have to be 8 or 9
        elif re.match(r"^04", m) and (len(m) < 8 or len(m) > 9):
            valid = False
        # if the number starts with 021, 022, 027 the total amount of letter have to be 10 or 11
        elif re.match(r"^(021|022|027|020)", m) and (len(m) < 10 or len(m) > 11):
            valid = False
    else:
        valid = False
    if valid:
        return m
    # if number doesn't appear valid, the user is asked to re-enter
    else:
        print("This doesn't appear to be a valid phone number")
        return get_phone()


def get_phone():
    phone = get_integers("what is your phone number")
    val_phone(phone)
    return phone


                    else:
                    # minuses pizzas if the amount of pizzas is more than 5
                    t[choice][1] -= quantity
                    print("Maximum amount of pizzas you can order is 5")
                    return add_pizza(t, c)